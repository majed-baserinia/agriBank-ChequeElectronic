export type Props = {
	hasTitle?: boolean;
	checkData: {
		sayad: string;
		serie: string;
		serial: string;
		amount: string;
		reason?: string;
		description: string;
		sheba?: string;
		date: string;
		checkStatus?: string;
	};
};
